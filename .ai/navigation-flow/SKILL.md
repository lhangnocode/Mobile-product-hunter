# Navigation Flow

## Rules

- Keep route names centralized if the project already has a navigation structure.
- Do not duplicate route strings across many files.
- Keep navigation actions explicit.
- Avoid passing large objects through navigation arguments.
- Prefer IDs or simple parameters.

## Before changing navigation

Check:
- existing routes
- screen entry points
- back stack behavior
- whether deep links are used
